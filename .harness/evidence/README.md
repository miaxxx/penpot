# Verification Evidence

`evidence.mjs` writes bounded JSON records here. Commit evidence that supports a
milestone or handoff; remove superseded noisy records during review.

Evidence files must never contain environment variables, API keys,
authorization headers, cookies or raw provider payloads.
